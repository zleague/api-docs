---
title: "Overview"
description: "The MEGA public CRM Lead API — pull leads, push leads, and receive lead webhooks."
---

The **MEGA public CRM Lead API** lets your systems integrate with your MEGA CRM:

- **Pull** — list and search your leads, with cursor pagination for incremental polling.
- **Push** — create leads one at a time, or import up to 500 at once.
- **Webhooks** — get notified in real time when a new lead is created, with HMAC-signed deliveries.

## Base URL

All endpoints live under:

```
https://app.gomega.ai
```

## Server-to-server only

These are **secret-key** endpoints. Call them from your backend — never from a browser or mobile app, and never embed your key in client-side code. There is no browser CORS access for the lead endpoints.

## Authentication in one line

Every request needs two things: your Personal Access Token and your customer id.

```bash
curl https://app.gomega.ai/api/agents/crm/leads \
  -H "Authorization: Bearer mega_your_token_here" \
  -H "x-customer-id: 00000000-0000-0000-0000-000000000000"
```

See [Authentication](/authentication) for how to get a key and which scopes each endpoint needs.

## What's here

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">Make your first request in a couple of minutes.</Card>
  <Card title="Pull leads" icon="download" href="/pull-leads">List, search, filter, and paginate leads.</Card>
  <Card title="Push leads" icon="upload" href="/push-leads">Create single leads or bulk-import them.</Card>
  <Card title="Webhooks" icon="webhook" href="/webhooks">Receive & verify `lead.created` events.</Card>
  <Card title="Rate limits" icon="gauge" href="/rate-limits">Limits, headers, and 429 handling.</Card>
  <Card title="Errors" icon="triangle-exclamation" href="/errors">The consistent error envelope.</Card>
</CardGroup>
